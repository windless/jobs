$('#review #work-<%= @work.id %>').remove();
$('#review-count').text($('#review ul').children().length);

$('#sprint-tab li:eq(3) a').tab('show');

$('#complete ul').append('<%= j render(@work) %>');
$('#complete-count').text($('#complete ul').children().length);
