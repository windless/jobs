$('#review #work-<%= @work.id %>').remove();
$('#review-count').text($('#review tr').length);

$('#sprint-tab li:eq(3) a').tab('show');

$('#complete table').append('<%= j render(@work) %>');
$('#complete-count').text($('#complete tr').length);
